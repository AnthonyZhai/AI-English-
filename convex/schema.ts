import { defineSchema } from "convex/server";

// Minimal schema — we primarily use Convex Storage for audio file hosting.
// Tables can be added here later as needed.
export default defineSchema({});
